{
  "name": "ng-env-demo",
  "version": "0.0.0",
  "scripts": {
    "start:demo": "cd apps/ngx-env-demo && yarn start",
    "start:cli": "lerna run start --scope=@ngx-env/builder",
    "build": "lerna run build"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}