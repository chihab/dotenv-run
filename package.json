{
  "name": "dotenv-run",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel --no-cache",
    "build": "turbo build"
  },
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.10.1",
    "typescript": "^5.1.3",
    "@changesets/cli": "^2.26.1"
  }
}