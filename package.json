{
  "name": "ng-env-demo",
  "version": "0.0.1",
  "scripts": {
    "start:demo": "lerna exec --scope=@ngx-env/demo -- yarn start",
    "start:cli": "lerna run start --scope=@ngx-env/cli",
    "start:lib": "lerna run start --scope=@ngx-env/core",
    "build": "lerna run build",
    "build:cli": "lerna run build --scope=@ngx-env/cli",
    "build:lib": "lerna run build --scope=@ngx-env/core"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
