{
  "$schema": "http://json-schema.org/schema",
  "$id": "ngx-env-ng-add",
  "title": "ngx-env schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": ["project"]
}
